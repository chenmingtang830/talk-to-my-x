# Bundles (runtime)

Exported feed packs land here as `latest.json` and timestamped copies.
Schema: `../references/bundle.schema.example.json`.

```bash
python3 scripts/bundle_tools.py export
python3 scripts/bundle_tools.py import bundles/latest.json
```
