# CLAUDE.md

This project is the **x-feed-loop** harness-agnostic agent skill.

Read and follow `SKILL.md`. Use the official X skill / `xurl` for X access and
the `x-feed-loop` CLI only for deterministic local state. Never publish without
showing the final draft and receiving explicit confirmation.
